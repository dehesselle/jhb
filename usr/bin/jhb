#!/usr/bin/env bash
#
# SPDX-FileCopyrightText: 2021 René de Hesselle <dehesselle@web.de>
#
# SPDX-License-Identifier: GPL-2.0-or-later

### description ################################################################

# This script is a wrapper for the jhbuild binary to include our configuration
# from etc/bootstrap.conf.d.

### shellcheck #################################################################

# Nothing here.

### includes ###################################################################

#---------------------------------------------------------- source configuration

# shellcheck disable=SC1090 # can't point to a single source here
for config in $(\
    "$(dirname "${BASH_SOURCE[0]}")"/run-parts list \
        "$(dirname "${BASH_SOURCE[0]}")"/../../etc/bootstrap.conf.d/'*.sh' \
    ); do
  source "$config"
done

### variables ##################################################################

# shellcheck disable=SC2034 # export not desired
PYTHON=$BIN_DIR/python3   # required for libxml2

### functions ##################################################################

# Nothing here.

### main #######################################################################

"$USR_DIR"/bin/jhbuild "$@"