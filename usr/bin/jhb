#!/usr/bin/env bash
#
# SPDX-FileCopyrightText: 2021 René de Hesselle <dehesselle@web.de>
#
# SPDX-License-Identifier: GPL-2.0-or-later

### description ################################################################

# This script is a wrapper around the jhbuild binary to run it in our
# configured environment (etc/jhb.conf).

### shellcheck #################################################################

# Nothing here.

### dependencies ###############################################################

#---------------------------------------------------------- source configuration

source "$(dirname "${BASH_SOURCE[0]}")"/../../etc/jhb.conf.sh

#------------------------------------------- source common functions from bash_d

# bash_d is already available (it's part of etc/jhb.conf)

bash_d_include echo
bash_d_include error

#--------------------------------------------------- source additional functions

# shellcheck source=../../usr/src/jhb/jhbuild.sh
source "$(dirname "${BASH_SOURCE[0]}")"/../../usr/src/jhb/jhbuild.sh

### variables ##################################################################

export PYTHON=$BIN_DIR/python3   # required for libxml2

# https://github.com/dehesselle/jhb
JHB_URL=https://github.com/dehesselle/jhb/releases/download/\
v$VERSION/jhb_v$VERSION.tar.xz

### functions ##################################################################

# Nothing here.

### main #######################################################################

if $CI; then   # break in CI, otherwise we get interactive prompt by JHBuild
  error_trace_enable
fi

case "$1" in
  install_moduleset)
    jhbuild_configure "$2"   # e.g. 'jhbuild/myapp.modules'
    ;;
  install_jhb)
    curl -L "$JHB_URL" | tar -C "$WRK_DIR" -xJ
    ;;
  debug)
    echo_d "doing nothing"
    ;;
  *)
    "$USR_DIR"/bin/jhbuild "$@"
    ;;
esac